# 第10章 多段パイプライン

> **到達目標（緑色帯 800〜1199）**: 貪欲・山登り・焼きなましを時間配分付きでつなぎ、各段のスコアログからボトルネックを特定できる。

---

## 10.1 貪欲 → 山登り → 焼きなまし の組み合わせ

**霊夢**: 第5章から第8章、バラバラに学んだけど、本番はどう組み合わせるの？

**魔理沙**: **パイプライン**だ。1つの手法に賭けず、**役割分担** させる。

```
[貪欲]  粗い解を速く作る（construct）
   ↓
[山登り] 確実に良くなる手だけ入れる（polish）
   ↓
[焼きなまし] 局所最適から抜けて伸ばす（explore）
```

| 段階 | 役割 | 第10章コード |
|------|------|-------------|
| 貪欲 | 初期解の質を底上げ | `greedy_schedule` |
| 山登り | 悪化なしで詰める | `hill_climb` |
| SA | 悪化受理で探索 | `sa` |

**霊夢**: 全部入れると遅くない？

**魔理沙**: **時間で区切る**から大丈夫。第10章の `pipeline.py` は **合計2秒** のうち、前半60%を construct（貪欲+山登り）、残りを SA に使う。

```python
# python/ch10/pipeline.py
TOTAL_SEC = 2.0
CONSTRUCT_RATIO = 0.6

t0 = time.perf_counter()
end = t0 + TOTAL_SEC
construct_end = t0 + TOTAL_SEC * CONSTRUCT_RATIO
```

```go
// go/ch10/pipeline.go
const (
	totalSec       = 2.0
	constructRatio = 0.6
)
```

**霊夢**: Intro HC と同じスコア関数？

**魔理沙**: 第8章と同じ **日程最適化**（`score_schedule`）。パイプラインは **問題に依存しない骨格** で、中身の `greedy` / `hill_climb` / `sa` だけ差し替えれば AHC011 系にも応用できる。

---

## 10.2 時間配分（construct 60% / improve 40% など）

**霊夢**: 60% / 40% は固定？

**魔理沙**: **出発点**だ。問題によって振る。

| 配分 | 向いている状況 |
|------|----------------|
| construct 長め（70%） | 初期解の質が勝負（配置・構築が難しい） |
| improve 長め（50%以上 SA） | 初期解はそこそこ、探索で伸びる |
| 山登り短め | 近傍が重い・改善が早く頭打ち |

```python
# 第10章のタイムライン（TOTAL_SEC=2.0 の例）
# 0.0s ─────────────── 1.2s ────── 2.0s
#      [    greedy + hill_climb    ][  SA  ]
#      ←── CONSTRUCT_RATIO=0.6 ──→
```

```python
print("phase=greedy start", file=sys.stderr)
state = greedy_schedule(problem)
print(f"phase=greedy end score={score_schedule(problem, state)}", file=sys.stderr)

print("phase=hill_climb start", file=sys.stderr)
state = hill_climb(problem, state, construct_end, rng)

print("phase=sa start", file=sys.stderr)
state = sa(problem, state, end, rng)
```

**霊夢**: 山登りは `construct_end` まで？

**魔理沙**: そう。SA は **`end` まで**。`time.perf_counter() < deadline` でループを切るので、**オーバーヘッド分は自分でマージン** を取るのが本番作法だ。

```python
while time.perf_counter() < deadline:
    # 1 step
```

**霊夢**: 貪欲は時間使わない？

**魔理沙**: 第10章の貪欲は **日ごとに26通り試す** 小さな D 向け。D=365 本番では貪欲にも締切を付けるか、もっと軽い construct（ランダム+SA）にする。

---

## 10.3 途中経過の保存と最終提出の選び方

**霊夢**: SA の途中でスコアが落ちたら、提出は下がる？

**魔理沙**: だから **best-so-far（歴代最高）** を別変数で持つ。第8章 SA と同じ思想をパイプライン全体に広げる。

| 変数 | 意味 |
|------|------|
| `current` / `state` | 今探索中の解（悪化もありうる） |
| `best` | 今まで見た最高スコアの解 |
| 提出 | **常に best**（または最終 `state` が best ならそれ） |

```python
# hill_climb: 受理は改善のみ → state が単調増加に近い
if trial_score > score:
    current = trial
    score = trial_score

# sa: best を更新
if score > best_score:
    best = current.copy()
    best_score = score
return best  # 最終的に best を返す
```

**霊夢**: 段階ごとのスコアも残した方がいい？

**魔理沙**: **必須に近い**。第10章は stderr にフェーズログを出す。

```text
phase=greedy start
phase=greedy end score=1079455
phase=hill_climb start
phase=hill_climb end score=1079455
phase=sa start
phase=sa end score=1079455
elapsed=2.000s
```

**霊夢**: この例だと山登りも SA も数字が同じに見えるの。

**魔理沙**: サンプル D=5 では **貪欲がすでに強く**、残り時間で頭打ちになりやすい。演習では **construct を弱くする**（ランダム初期解）などして差を出す。

```python
# ボトルネック調査用メモテンプレート
# | phase      | end_score | 所要時間(概算) |
# | greedy     |           |                |
# | hill_climb |           |                |
# | sa         |           |                |
```

**霊夢**: 複数回実行したらどれを提出？

**魔理沙**: **スコア最大の1本**。シード違いの multi-start なら、各 run の best を比較して最高を選ぶ（第6章 multi-start の延長）。

---

## 10.4 ハンズオン: AHC011「Sliding Tree Puzzle」— 初期解生成 + SA

**霊夢**: パズル問題なの？ コードは日程だけど。

**魔理沙**: **概念の橋渡し**だ。[AHC011](https://atcoder.jp/contests/ahc011/tasks/ahc011_a) は **木をスライドして目標配置に近づける** 典型で、本番解法はだいたい次の形になる。

```
1. 構築: 貪欲 / ビーム / ランダムで操作列 or 配置の初期解
2. 改善: 山登りで確実に良化
3. 仕上げ: SA で盤面を抜け出す
```

第10章コードは **同じパイプライン骨格** を、手元で検証しやすい Intro HC 型スコア（`data/ch10/sample.txt`）に載せ替えている。AHC011 に応用するときは `greedy_schedule` と近傍を **スライド操作** に差し替える。

### Step 1: 実行（2秒・フェーズログ）

```bash
cd /path/to/yukkuri-heuristics
python python/ch10/pipeline.py < data/ch10/sample.txt
```

stderr 先頭の例:

```text
phase=greedy start
phase=greedy end score=1079455
phase=hill_climb start
hc step=200 score=1079455
...
phase=sa start
sa step=500 score=... T=...
phase=sa end score=1079455
elapsed=2.000s
```

stdout: 5行のタイプ（例）

```text
1
17
13
14
20
```

### Step 2: Go

```bash
go run go/ch10/pipeline.go < data/ch10/sample.txt
```

### Step 3: 時間配分の改変

```python
CONSTRUCT_RATIO = 0.3  # construct 短く → SA に時間を回す
CONSTRUCT_RATIO = 0.8  # 山登り長く → 初期解重視
```

各設定で `phase=* end score=` を記録する。

### Step 4: 段階を1つ外す実験

```python
# hill_climb をスキップして greedy → sa だけにする
# sa をコメントアウトして greedy → hill_climb だけにする
```

どの段でスコアが伸びたか表にまとめる（10.5 演習）。

### Step 5: AHC011 本番への対応表

| パイプライン段 | AHC011 での例 |
|---------------|--------------|
| 貪欲 | 木を目標に近づける操作を貪欲に積む |
| 山登り | 1手戻し・スワップで改善のみ |
| SA | 悪手を含む操作列のランダム変更 |
| 時間配分 | 構築にビーム、残り SA など |

公式 `tools` のビジュアライザで **木の連結性** と **スコア** を確認しながら、第9章ビーム＋第10章パイプラインを合成するのが緑〜水色の定番ルートだ。

---

## 演習

1. `CONSTRUCT_RATIO` を `0.3 / 0.6 / 0.8` で振り、**各 `phase=* end score=`** と `elapsed=` を表にする。
2. 初期解を `greedy_schedule` ではなく **ランダム**（第8章と同様）にし、3段階の伸び方を比較する。
3. stderr の `hc step=` / `sa step=` をファイルにリダイレクトし、**どの段階で best が最後に更新されたか** を特定する。

```bash
python python/ch10/pipeline.py < data/ch10/sample.txt 2> pipeline.log
grep 'phase=' pipeline.log
grep 'best' pipeline.log  # 必要ならコード側に best 更新ログを追加
```

4. 第9章 `beam_fill.py` で得た経路を「初期解」とみなす案を書き、パイプラインに組み込むには何が要るか箇条書きにする。
5. AHC011 の問題文を読み、**State / Move / Score** を第3章テンプレートで埋める（実装は次の帯でも可）。

---

## 章末まとめ

**霊夢**: 貪欲で土台、山登りで整えて、SA で伸ばす——流れがはっきりしたの。

**魔理沙**: まとめだ。

> **第10章まとめ**
>
> - 本番は **貪欲 → 山登り → SA** の多段パイプラインが定番
> - **時間配分**（例: construct 60% / improve 40%）は問題で調整する
> - 提出は **best-so-far**。フェーズごとの stderr ログでボトルネックを見る
> - ハンズオン: `python/ch10/pipeline.py`（2秒・フェーズログ）/ AHC011 の概念
> - 第3部（緑）の締め: **SA・ビーム・パイプライン** を組み合わせて戦う
> - 次部（水色）: インタラクティブ・タブー・データ構造高速化（第11章〜）

**霊夢**: `phase=greedy end score=` を見ると、どこに時間を足すか決めやすいのね。

**魔理沙**: ログ無しのパイプラインは **闇雲チューニング** になる。緑帯のうちに **「段階別スコア＋残り時間」** を stderr に吐く癖を付けろ。水色帯ではインタラクティブや差分更新と組み合わせて、同じ骨格で本番問題に載せ替えるんだぜ。
