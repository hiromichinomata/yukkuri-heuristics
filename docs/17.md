# 第17章 マルチエージェント問題

> **到達目標（青色帯 1600〜1999）**: 複数主体の **状態・手番・衝突** をモデル化し、協調と競合を分けて設計できる。[AHC008](https://atcoder.jp/contests/ahc008)「Territory」級の問題を貪欲＋手番管理で骨格化できる。

---

## 17.1 複数主体の状態管理

**霊夢**: 今まで「1 つの解」だったけど、エージェントが2人いたら状態が2倍？

**魔理沙**: 倍どころじゃない。**グローバル状態** に全部載せる。

| 変数 | 意味（第17章） |
|------|----------------|
| `owner[r][c]` | マス `-1` / `0` / `1` の所有者 |
| `positions[agent]` | 各エージェントの現在地 |
| `scores[agent]` | 累積スコア |
| `turn` | 何手目か（手番は `turn % 2`） |

```python
# python/ch17/territory_greedy.py
owner = [[-1] * n for _ in range(n)]
positions = list(problem.starts)
scores = [0, 0]
```

**霊夢**: 初期位置は？

**魔理沙**: 入力の `starts` で与える。開始マスは即占領し、そのマスの `grid` 値をスコアに加算する。

```python
for agent in range(2):
    r, c = positions[agent]
    owner[r][c] = agent
    scores[agent] += problem.grid[r][c]
```

AHC008 本番ではエージェント数・ターン上限・移動ルールが問題文で定義される。第17章は **2 体・4 近傍・未占領マスのみ進入** の縮約版だ。

---

## 17.2 行動スケジューリングと衝突回避

**霊夢**: 同じマスに二人入ったら？

**魔理沙**: **未占領 (`owner == -1`) だけ選ぶ** ことで衝突を防ぐ。本番 Territory では移動・占領・建設などルールが細かいが、考え方は同じ——**合法手集合** を毎ターン再計算する。

```python
DIRS = ((-1, 0), (1, 0), (0, -1), (0, 1))

def best_move(n, grid, owner, agent, pos):
    for dr, dc in DIRS:
        r, c = r0 + dr, c0 + dc
        if owner[r][c] != -1:
            continue  # 衝突回避
        val = grid[r][c]
        # 最大 val のマス（同点は辞書順で小さい座標）
```

**霊夢**: 手番はどう回すの？

**魔理沙**: **交互ターン**。`agent = turn % 2` で、動けなければ `stagnant` を増やし、両者とも動けない状態で終了する。

```python
while stagnant < 2:
    agent = turn % 2
    mv = best_move(...)
    if mv is None:
        stagnant += 1
        turn += 1
        continue
    stagnant = 0
    # 占領して turn += 1
```

| 設計の落とし穴 | 対策 |
|----------------|------|
| 同マス二重占領 | 進入前に `owner` を確認 |
| 無限ループ | パス連続で打ち切り |
| 手番の取りこぼし | `turn` と `agent` を常に対で更新 |

---

## 17.3 協調と競合のモデリング

**霊夢**: 2 人は協力するの？ 奪い合うの？

**魔理沙**: Territory は **競合** が主役だ。自分のスコアを最大化しつつ、相手の伸びを **ブロック** する手が強い。協調が要る問題（チームで目標達成）では **共有スコア** や **役割分担** を状態に足す。

| 観点 | 競合（AHC008 系） | 協調（例） |
|------|-------------------|-----------|
| 目的関数 | 個別スコア | 合計・制約充足 |
| 手の評価 | 自分の利得 − 相手利得の近似 | チーム利得 |
| 探索 | ミニマックス・MCTS（第19章） | 共同計画 |

**霊夢**: 貪欲だけだと弱い？

**魔理沙**: 1 手先だけ見ると **相手に取られる高得点マス** を見落とす。青色では **2〜3 手読み**、終盤の **完全探索の枝刈り**、第18章の **SA で行動列全体を焼く** などに伸ばす。

### 状態の実装チェックリスト

読者が自分の Territory 解法を書くとき、次を **1 ファイルの struct / dataclass** にまとめるとバグが減る。

1. `grid`（得点）と `owner`（占有）を混同しない  
2. 移動後に **必ず `owner` と `scores` を同時更新**  
3. 手番インデックスと **ログ出力の agent ID** を一致させる  
4. 終了条件（パス・ターン上限）を問題文と同型にする  

```go
// go/ch17/territory_greedy.go — solve の流れは Python と同型
for stagnant < 2 {
    agent := turn % 2
    mv := bestMove(...)
    // ...
}
```

**霊夢**: 出力形式は？

**魔理沙**: 第17章サンプルは **スコア2行 + 操作ログ**。本番 AHC008 は公式の出力仕様に合わせる。ハンズオンでは **stderr に集計、stdout に提出相当** と分ける（第0章の I/O テンプレート）。

### シミュレーション駆動デバッグ

| 確認項目 | 方法 |
|---------|------|
| 非法占領 | `owner[r][c]` が二重に0/1にならないか |
| スコア合計 | 移動のたびに `grid` 加算が1回だけか |
| 手番 | `moves_log` の agent が交互か |

`data/ch17/sample.txt` は 6×6 で開始 `(0,0)` と `(5,5)`。対称な盤面のため **同点スコア** になりやすい——評価関数を変えると差が出る（17.5）。

---

## 17.4 ハンズオン: AHC008「Territory」— エージェント行動の設計

**霊夢**: 本番問題そのままじゃないんだ。

**魔理沙**: 骨格は同じ。**グリッド + 複数エージェント + 交互手番 + 貪欲移動**。コードは `territory_greedy`、入力は `data/ch17/sample.txt`。

```
6              # n
(6 lines grid)
0 0            # agent 0 start
5 5            # agent 1 start
```

### Step 1: 実行

```bash
python python/ch17/territory_greedy.py < data/ch17/sample.txt
go run go/ch17/territory_greedy.go < data/ch17/sample.txt
```

stderr 例: `territory0=38 territory1=38 moves=20`。stdout は `score0 score1` のあと `agent r c` のログ。

### Step 2: 評価関数の改変

`best_move` の比較を変える:

```python
# 案A: 現状 — grid 値最大
# 案B: grid + 隣接未占領数（伸びしろ）
# 案C: grid - 相手が次に取れそうな最大値（簡易防御）
```

各案で `territory0`, `territory1` を記録する。

### Step 3: 手番順の実験

- **交互（現状）**: `turn % 2`
- **1 エージェントずつ全ターン**: エージェント0が動けなくなるまで0のみ動かす（17.5 演習）

### Step 4: AHC008 本番へのマッピング

| 第17章 | AHC008 Territory（概念） |
|--------|-------------------------|
| `owner` | 陣地・建物の占有 |
| `best_move` | 移動先・行動種別の選択 |
| 交互ターン | 公式のターン進行 |
| stderr スコア | ビジュアライザで検証 |

公式ページ: [AHC008](https://atcoder.jp/contests/ahc008)。ビジュアライザで **手番ごとの陣地拡大** を目視すると、貪欲の弱点が見える。

### Step 5: 第19章 MCTS への橋渡し

Territory の1手を「ノード＝盤面＋手番」とみなせば **MCTS の層** になる。第19章 `mcts_toy` はグラフ上の対戦だが、**UCB1 で手を選ぶ流れ** は同じ。貪欲 `best_move` を rollout ポリシーにすれば、**MCTS + 貪欲 rollout** の原型になる。

---

## 17.5 演習

1. **評価案 B/C** を実装し、同一 `sample.txt` で `(territory0, territory1)` を比較する。
2. **エージェント0だけ** が最大 `T` 手動くモードを追加し、交互ターンとの差を 3 行で説明する。
3. 動画・editorial を読み、上位解法が **貪欲 / ビーム / SA / シミュレーション** のどれを軸にしていたか推測し、箇条書きにする。

```bash
python python/ch17/territory_greedy.py < data/ch17/sample.txt 2> ter.log
grep territory ter.log
go run go/ch17/territory_greedy.go < data/ch17/sample.txt 2>&1 | head -3
```

4. `DIRS` を斜め4方向に増やしたとき、`moves` 数とスコアがどう変わるか予測し、実行と照合する。

---

## 章末まとめ

**霊夢**: 状態はグローバルに持って、手番で回す——整理できた。

**魔理沙**: まとめだ。

> **第17章まとめ**
>
> - マルチエージェントは **`owner`・位置・スコア・手番`** を一体で更新する
> - **合法手** と **衝突回避** を毎ターン再計算する
> - 競合問題では **相手の利得** を見た手が強い（貪欲だけでは足りることが多い）
> - ハンズオン: `territory_greedy` / [AHC008](https://atcoder.jp/contests/ahc008) の縮約版
> - 次章: **高度 SA**（[AHC015](https://atcoder.jp/contests/ahc015) 系・複数近傍）

**霊夢**: 配置は焼きなましの出番ね。

**魔理沙**: 第8章の SA に **近傍ポートフォリオ・再ヒート** を足す。青色の締めに持っていくぜ。
