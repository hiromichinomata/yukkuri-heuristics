# 第8章 焼きなまし法（Simulated Annealing）

> **到達目標（緑色帯 800〜1199）**: 局所最適から抜け、温度スケジュールと近傍設計を調整してスコアを伸ばせる。

---

## 8.1 局所最適から抜け出す発想

**霊夢**: 第6章の山登り、悪い手は絶対に受け入れないんでしょ？ そしたら谷から出られないのでは？

**魔理沙**: その通りだ。**山登り（Hill Climbing）** は「今より良い解だけ採用」するから、**局所最適**に張り付く。

**霊夢**: 局所最適って？

**魔理沙**: **近傍のどれを試しても改善しない解**のことだ。全体ではもっと良い解があるのに、1歩ずつ変えるだけでは届かない谷に落ちる。

```
        スコア
          ^
          |     *  ← 大域的に良い山頂
          |    / \
          |   /   \
          |  /     \___*___  ← 局所最適（ここから脱出できない）
          | /            \
          +------------------→ 解空間
```

**霊夢**: 焼きなましはどうやって抜けるの？

**魔理沙**: **たまに悪化も受け入れる**。受け入れ確率は **温度 T** で決める。T が高いうちは悪手も通しやすく、冷えるほど山登りに近づく——金属を焼いてゆっくり冷やすイメージだぜ。

| 手法 | 悪化の受理 | 局所最適 |
|------|-----------|----------|
| 山登り | しない | 抜けにくい |
| 焼きなまし | 確率的にする | 抜けやすい |

```python
# 受理の核心（delta = 新スコア - 今スコア）
import math

def accept(delta: int, temperature: float, rng) -> bool:
    if delta >= 0:
        return True  # 改善は必ず採用
    # 悪化: exp(delta / T) の確率で採用（delta < 0 なので指数 < 1）
    return rng.random() < math.exp(delta / temperature)
```

```go
// 同じ判定（Go）
func accept(delta float64, temperature float64, rng *rand.Rand) bool {
	if delta >= 0 {
		return true
	}
	return rng.Float64() < math.Exp(delta/temperature)
}
```

**霊夢**: delta がマイナスだと exp は 1 未満になるから、運でたまに通るのね。

**魔理沙**: そう。**T が大きいほど**悪化も通りやすい。**T → 0** に近づくと山登りと同じになる。AHC ではこの「確率的な悪化受理」が緑帯の必須武器だ。

---

## 8.2 温度スケジュール（初期温度・冷却率・終了条件）

**霊夢**: 温度はどう決めるの？ 適当に下げればいい？

**魔理沙**: 3つセットで決める。**初期温度 T0**、**冷却率 ALPHA**、**終了条件**だ。

| パラメータ | 役割 | 本リポジトリの例 |
|-----------|------|------------------|
| `T0` | 探索初期の「大胆さ」 | テンプレート `100`、第8章本番 `5000` |
| `ALPHA` | 1ステップごとの冷却（0〜1） | `0.995`（両方共通） |
| 終了 | いつ止めるか | **残り時間**（コンテストではこれが正義） |

**霊夢**: T0 がテンプレートと第8章で違うのはなぜ？

**魔理沙**: **スコアのスケール**が違うからだ。`python/templates/sa_template.py` はデモ用に小さな整数、`sa_intro_hc.py` は Intro HC のスコア（百万単位）向けに **T0=5000** にしている。

```python
# python/templates/sa_template.py — 汎用テンプレ（T0=100, ALPHA=0.995）
T0 = 100.0
ALPHA = 0.995

def sa(initial_state, neighbor_fn, score_fn, time_limit_sec=2.0, rng=None):
    # ...
    T = T0
    while time.perf_counter() - start < time_limit_sec:
        # ...
        if delta >= 0 or rng.random() < math.exp(delta / T):
            state = new_state
            score = new_score
        T *= ALPHA  # 幾何冷却
```

```python
# python/ch08/sa_intro_hc.py — Intro HC 向け（T0=5000, ALPHA=0.995）
T0 = 5000.0
ALPHA = 0.995
TIME_LIMIT_SEC = 1.5

# ループ内
temperature *= ALPHA
```

**霊夢**: ALPHA=0.995 って、1回で 0.5% 冷える？

**魔理沙**: そう。**幾何冷却** `T ← T × ALPHA` が定番。ステップ数が多いと最後はほぼ 0 になる。

```text
# ステップ n 後の温度（概算）
T(n) = T0 * ALPHA^n

# 例: T0=5000, ALPHA=0.995
n=500   → T ≈ 40.8
n=1000  → T ≈ 0.33
n=1500  → T ≈ 0.003
```

**霊夢**: 終了条件は「ステップ数」じゃなく時間？

**魔理沙**: AHC では **`time.perf_counter()` と締切** が基本。ステップ上限だけだと、近傍が重い問題で TL を超える。

```python
start = time.perf_counter()
while time.perf_counter() - start < time_limit_sec:
    # 1 iteration
```

**霊夢**: 初期温度の決め方は？

**魔理沙**: 経験則で **「悪化 Δ の典型値を、初期段階で 30〜70% くらいの確率で受理できる T」** を探す。本番では stderr の `T=` と `score=` を見ながら `T0` を振る（8.6 演習）。

---

## 8.3 近傍設計のコツ（小さく変える / 大きく変える）

**霊夢**: 近傍（Neighborhood）って、第6章と同じ？

**魔理沙**: 同じ概念だ。**今の解から少しだけ変えた解の集合**が近傍。SA ではここから **ランダムに1つ** 引いて試す。

| 近傍の大きさ | 例（Intro HC 日程） | 向いている局面 |
|-------------|---------------------|----------------|
| 小 | 1日だけタイプを変える | 仕上げ・微調整 |
| 中 | 2日スワップ、連続区間を反転 | 中盤の探索 |
| 大 | 全日をランダム再生成 | 初期解が酷いとき（別系統） |

```python
# python/ch08/sa_intro_hc.py — 小さな近傍
def random_neighbor(state: ScheduleState, rng: random.Random) -> ScheduleState:
    day = rng.randrange(len(state.types))
    new_type = rng.randint(1, 26)
    nxt = ScheduleState(list(state.types))
    nxt.types[day] = new_type
    return nxt
```

```go
// go/ch08/sa_intro_hc.go
func randomNeighbor(state ScheduleState, rng *rand.Rand) ScheduleState {
	nxt := append([]int{}, state.Types...)
	day := rng.Intn(len(nxt))
	nxt[day] = rng.Intn(26) + 1
	return ScheduleState{nxt}
}
```

**霊夢**: 小さい近傍だけだと遅くない？

**魔理沙**: 1ステップは速いが、大きな改善には **多ステップ** が要る。だから **温度が高いうち** にたまに悪化して谷を越え、**冷えてから** 小さな近傍で詰める、が典型だ。

**霊夢**: 大きい近傍はいつ使う？

**魔理沙**: **複数の neighbor_fn を混ぜる**（確率 0.9 で1日変更、0.1 でスワップなど）か、**パイプライン**で貪欲→山登り→SA（第10章）に任せる。1ファイルに全部詰めなくてもいい。

```python
def mixed_neighbor(state, rng):
    if rng.random() < 0.9:
        return random_neighbor(state, rng)
    return swap_two_days_neighbor(state, rng)
```

---

## 8.4 状態のコピー vs 差分更新

**霊夢**: 毎回 `list(state.types)` してコピーしてるけど、遅くない？

**魔理沙**: **正しくて速い実装**なら差分更新が最強だが、まずは **コピーで動かす** のが安全。Intro HC の5日サンプルならコピーで十分。

| 方式 | メリット | デメリット |
|------|---------|-----------|
| **コピー** | バグが少ない、読みやすい | メモリ・コピーコスト |
| **差分更新** | スコア再計算を O(1)〜O(log N) にできる | 実装が難しい、ロールバックが要る |

```python
# コピー型（第8章）
nxt = ScheduleState(list(state.types))
nxt.types[day] = new_type
new_score = score_schedule(problem, nxt)  # 全再計算

# 差分更新のイメージ（第13章で本格化）
# delta_score = f(変更した日だけ) で score += delta_score
```

```python
# 現在解を「破壊的に」更新するパターン（ロールバック必須）
def try_move_inplace(state, day, new_type):
    old = state.types[day]
    state.types[day] = new_type
    new_score = score_schedule(problem, state)
    if not accept(...):
        state.types[day] = old  # 戻す
    return new_score
```

**霊夢**: SA では best を別に持ってるのね。

**魔理沙**: 重要だ。**現在解 state** は悪化も含めて動く。**best_state** は歴代最高だけ保存。最終出力は **best** を出す（現在が一時的に悪化していても提出はベスト）。

```python
best_state = ScheduleState(list(state.types))
best_score = score

# ループ内
if score > best_score:
    best_score = score
    best_state = ScheduleState(list(state.types))
```

---

## 8.5 ハンズオン: Introduction to Heuristics Contest C 問題 — SA による改善

**霊夢**: 実際に動かすの？

**魔理沙**: [Intro HC 問題C（Optimization）](https://atcoder.jp/contests/intro-heuristics/tasks/intro_heuristics_c) と同型の入力で、**365日スケジュールを SA で改善**する。本リポジトリでは日数 `D` を小さくした `data/ch08/sample.txt`（D=5）で検証する。

### 問題のおさらい（State / Move / Score）

| 用語 | 内容 |
|------|------|
| State | 各日のコンテストタイプ `t_1..t_D`（1〜26） |
| Move | ある日のタイプを別の値に変える |
| Score | `max(10^6 + 最終満足度, 0)`（問題A/B と同じ減衰モデル） |

### リポジトリ構成

```text
python/ch08/sa_intro_hc.py
go/ch08/sa_intro_hc.go
data/ch08/sample.txt
python/templates/sa_template.py   # 汎用 SA（T0=100）
```

### Step 1: 実行（Python）

```bash
cd /path/to/yukkuri-heuristics
python python/ch08/sa_intro_hc.py < data/ch08/sample.txt
```

stderr の例（seed=42、環境により多少変動）:

```text
initial score=1025008
sa step=500 score=1077868 T=407.86
sa step=1000 score=1079455 T=33.27
sa step=1500 score=1079455 T=2.71
best score=1079507
```

stdout: D 行のタイプ（提出用）

```text
1
17
9
14
13
```

**霊夢**: 初期 1025008 から best 1079507 まで上がってるのね。

### Step 2: Go でも同じ入力

```bash
go run go/ch08/sa_intro_hc.go < data/ch08/sample.txt
```

### Step 3: テンプレートで SA の形を体得する

```bash
python python/templates/sa_template.py
# → best_state=42 付近（1次元デモ）
```

テンプレートは **neighbor_fn / score_fn を差し替えるだけ** で他問題に流用できる。

```python
# 使い方の型
best_state, best_score = sa(
    initial_state,
    neighbor_fn,
    score_fn,
    time_limit_sec=2.0,
    rng=random.Random(42),
)
```

### Step 4: 改変（1パラメータずつ）

```bash
# T0 を下げる → 初期は保守的、谷を越えにくい
# sa_intro_hc.py の T0 = 500.0 に変更して再実行

# ALPHA を 0.99 に → 冷却が速い、後半は山登り寄り
# ALPHA = 0.99 に変更して stderr の T= の減り方を比較
```

### Step 5: コンテスト形式（2秒・best 提出）

- `TIME_LIMIT_SEC` を `1.95` などにし、**stderr はログ、stdout は解のみ** にする
- ループ中は **best_score だけ** を更新し、最後に `best_state` を出力（第8章コードは既にこの形）

**霊夢**: Intro HC 本番は D=365 だから、このサンプルより重い？

**魔理沙**: その通り。`score_schedule` の全再計算は O(26·D) なので、本番では **差分更新** や **近傍の軽量化** を検討する（第13章）。まずは **コピー + 時間制限 SA** で動かすことが先だ。

---

## 演習

1. `T0` を `500` / `5000` / `50000` に変え、**initial → best** の差と stderr の `T=` の減り方を表にまとめる。
2. `ALPHA` を `0.99` と `0.999` で比較し、**同じ 1.5 秒で到達する best_score** を記録する。
3. 近傍を「2日スワップ」に追加し、受理率とスコアの変化を観察する（`random_neighbor` を拡張）。
4. `python/templates/sa_template.py` の `target` を変え、**T0=100** で探索が成功する条件を言語化する。
5. Intro HC 問題C の公式入力（D=365）が手元にあれば、同じ `sa_intro_hc.py` に流し込み、第6章の山登りだけとスコアを比較する。

---

## 章末まとめ

**霊夢**: 悪化も受け入れるのが焼きなましの肝なのね。

**魔理沙**: まとめだ。

> **第8章まとめ**
>
> - **山登り**は局所最適に張り付く。**SA**は温度付きで悪化も確率的に受理する
> - **温度スケジュール**: `T0`（初期大胆さ）、`ALPHA`（幾何冷却）、**時間で終了**
> - 本リポジトリ: テンプレ `T0=100`、第8章 `T0=5000`、`ALPHA=0.995`
> - **近傍**は小さく始め、必要なら混ぜる。出力は **best_state** を使う
> - 実装はまず **コピー + 全スコア再計算**、速くなったら差分更新（第13章）
> - ハンズオン: `python/ch08/sa_intro_hc.py` / Intro HC 問題C
> - 次章（第9章）: ビームサーチ（AHC002 簡略版）

**霊夢**: stderr で温度とスコアを見るの、チューニングに便利なのだ。

**魔理沙**: SA はパラメータ競技でもある。**ログを見て T0 と ALPHA を振る**習慣をつけろ。緑帯では「SA を1回動かす」だけでなく、「なぜ伸びたか」を説明できるようにするんだぜ。
