# 第11章 インタラクティブ問題

> **到達目標（水色帯 1200〜1599）**: インタラクティブ入出力を正しく回し、クエリごとに推定を更新するオンライン解法を設計できる。

---

## 11.1 インタラクティブ問題の入出力形式

**霊夢**: インタラクティブって、ジャッジと会話しながら解くタイプなの？

**魔理沙**: そうだ。**提出プログラム**がクエリを stdout に出し、**ジャッジ**が stdin で答える。AHC003「Shortest Path Queries」が代表例だ。

| 役割 | 流れ |
|------|------|
| あなた | `PATH` などで経路を宣言 |
| ジャッジ | 真の距離・真の経路を `FEEDBACK` で返す |
| 繰り返し | クエリ回数ぶん |

**霊夢**: 普通の「入力を全部読んでから出力」と何が違うの？

**魔理沙**: **途中で flush して待つ**点だ。Python なら `print(..., flush=True)` か `sys.stdout.flush()`。Go なら `fmt.Println` のあとバッファに注意（`bufio` で遅延しないよう設計）。

```python
# python/ch11/interactive_path.py — インタラクティブ本体
def run_interactive() -> None:
    # ...
    for _ in range(q):
        s, t = map(int, next(it).split())
        pred, path = solver.answer(s, t)
        out_lines.append(f"PATH {pred:.4f} {len(path)} " + " ".join(map(str, path)))
        actual_line = next(it)  # ジャッジから FEEDBACK
        parts = actual_line.split()
        actual = float(parts[1])
        # ...
        solver.feedback(true_path, pred, actual)
    sys.stdout.write("\n".join(out_lines) + "\n")
    sys.stdout.flush()
```

```go
// go/ch11/interactive_path.go — 同じ対話ループ
for qi := 0; qi < q; qi++ {
    // s, t を読む
    pred, path := dijkstra(s.adj(), sv, tv)
    fmt.Printf("PATH %.4f %d", pred, len(path))
    // ...
    sc.Scan() // FEEDBACK 行
    updateEst(s.est, truePath, pred, actual)
}
```

**霊夢**: オフライン検証用の `--offline` もあるのね。

**魔理沙**: `data/ch11/sample.txt` に **真の重み** を入れておき、同じ更新ロジックをローカルで回す。本番前のデバッグに必須だぜ。

---

## 11.2 オンラインアルゴリズムの考え方

**霊夢**: オンラインって、未来のクエリが見えないってこと？

**魔理沙**: その通り。**今のクエリに答える時点では、後のクエリも真の辺の重みも分からない**。だから「今わかっている情報だけ」で最善を尽くし、**フィードバックで信念を更新**する。

```
  時刻 →
  ┌─────────┐    ┌─────────┐    ┌─────────┐
  │ Query 1 │ →  │ Query 2 │ →  │ Query 3 │
  │ 推定で答│    │ 更新後  │    │ さらに  │
  └─────────┘    └─────────┘    └─────────┘
       ↑              ↑
    FEEDBACK      FEEDBACK
```

**霊夢**: 最初は辺の重みが全部 1.0 なの？

**魔理沙**: 本リポジトリの `Solver` は **トポロジだけ** ジャッジから受け取り、重みは **推定 `est`** で持つ。

```python
class Solver:
    def __init__(self, n: int, topo: List[Tuple[int, int]]) -> None:
        self.est: Dict[Tuple[int, int], float] = {edge_key(u, v): 1.0 for u, v in topo}
```

**霊夢**: 真のグラフと推定グラフがズレてると、ダイクストラの答えもズレるわけね。

**魔理沙**: そう。スコアは「予測距離と真の距離の差」など問題依存だが、**推定を真に近づける更新**が本体になる。

---

## 11.3 探索と利用のバランス（多腕バンディットの入門）

**霊夢**: 多腕バンディットって、スロット機の比喩？

**魔理沙**: **複数の選択肢（腕）** から毎ターン1つ選び、報酬をもらう模型だ。AHC003 では「どの辺を信じるか」「どの経路を試すか」に対応づけられる。

| 方針 | 意味 | インタラクティブでの例 |
|------|------|------------------------|
| **利用（Exploit）** | 今いちばん良さそうな手 | 推定重みで最短経路を出す |
| **探索（Explore）** | 情報が少ない所を試す | あまり使わない辺を通る経路を意図的に選ぶ |

**霊夢**: 第11章のコードは探索してない？

**魔理沙**: サンプルは **利用寄り**（推定でダイクストラ）＋ **フィードバックで全経路の辺を比例更新** だ。探索を足すなら「不確かな辺を通るクエリ」を選ぶ **Thompson sampling / UCB** が次のステップ（第25章）だぜ。

```python
def update_estimates(est, path, predicted, actual):
    ratio = actual / predicted  # 予測が外れたら辺重みを一括スケール
    for i in range(len(path) - 1):
        k = edge_key(path[i], path[i + 1])
        est[k] = max(0.1, est.get(k, 1.0) * ratio)
```

**霊夢**: `ratio > 1` なら経路が想定より長かったから、辺を太くするイメージ？

**魔理沙**: まさに。**通った辺すべて** に同じ比率を掛ける簡略版だ。本番 AHC003 ではより細かい更新式・クリップ・正規化が要る。

---

## 11.4 クエリ回数・情報更新の設計

**霊夢**: クエリは何回でも打てるわけじゃないのよね？

**魔理沙**: **クエリ上限 Q** と **1クエリのコスト**（時間・スコア）をセットで設計する。

| 設計ポイント | 内容 |
|-------------|------|
| 更新の粒度 | 経路全体か、辺1本か |
| 数値の安定 | `max(0.1, w)` のような下限 |
| 再計算コスト | ダイクストラは `O((V+E) log V)` — 頂点数に注意 |
| キャッシュ | 同じ (s,t) を繰り返すならメモ化 |

**霊夢**: ダイクストラは Python が heapq、Go は配列走査？

**魔理沙**: 本リポジトリは **サンプルが小さい** ので Go はシンプルなダイクストラ、Python は `heapq` だ。頂点が増えたら **両方とも優先度キュー** に揃えろ。

```python
def dijkstra(adj, start, goal):
    pq = [(0.0, start)]
    while pq:
        d, u = heapq.heappop(pq)
        if d > dist[u]:
            continue
        # ...
```

**霊夢**: `edge_key` で `(u,v)` と `(v,u)` を統一してるのはなぜ？

**魔理沙**: 無向辺だから **辞書のキーを一意** にしないと、同じ辺を二重に更新してしまう。

---

## 11.5 ハンズオン: AHC003「Shortest Path Queries」— 重み更新 + ダイクストラ

**霊夢**: 手を動かす章だ。何から実行すればいい？

**魔理沙**: 4ステップだ。**読む → 写す → 改変 → コンテスト形式**。

### Step 1: オフラインで推定更新を確認

```bash
cd /path/to/yukkuri-heuristics
python python/ch11/interactive_path.py --offline data/ch11/sample.txt
```

期待する末尾（検証済み）:

```text
queries=3 sum_pred=7.00 sum_true=19.00
```

**霊夢**: 予測の合計 7 で真の合計 19？ 最初は重み 1.0 だから外れるのね。

**魔理沙**: 3クエリ後に `est` が更新され、次のクエリでは少しマシになる。ログの `pred=` / `true=` をクエリごとに見ろ。

### Step 2: 疑似ジャッジでインタラクティブ形式を再現

```bash
python python/ch11/query_simulator.py data/ch11/sample.txt
```

`query_simulator.py` が `data/ch11/sample.txt` から **FEEDBACK 付き入力** を組み立て、`interactive_path.py` を subprocess で起動する。

### Step 3: Go でも同じオフライン

```bash
go run go/ch11/interactive_path.go --offline data/ch11/sample.txt
```

### Step 4: 改変 — 更新比率のクリップ

`update_estimates` の `ratio` を `min(2.0, max(0.5, ratio))` にクリップし、`sum_pred` と `sum_true` の差がどう変わるかメモする。

### データ形式（`data/ch11/sample.txt`）

```text
6 9          # n, m
0 1 4.0      # 真の重み（オフライン検証用）
...
3            # クエリ数
0 5
1 4
2 5
```

インタラクティブ本番では **重み行は来ない**（トポロジのみ）。オフライン用に重み付きで保存している点に注意だ。

---

## 11.6 演習

1. クエリ順を `(2,5), (0,5), (1,4)` に入れ替え、`sum_pred` / `sum_true` がどう変わるか比較する。
2. `est` の初期値を `1.0` ではなく **全辺 2.0** にし、1クエリ目の経路が変わるか観察する。
3. `query_simulator.py` の `build_judge_input` を読み、**FEEDBACK 行のフォーマット** を紙に書き写す。
4. 探索寄りに、2クエリ目だけ「推定ではなく真の adj で最短」を選ぶダミー戦略を入れ、オンラインの不利を体感する。
5. AHC003 の公式問題文を開き、**スコアが予測誤差のどの関数か** を第3章のテンプレで整理する。

---

## 章末まとめ

**霊夢**: ジャッジと flush で会話し、フィードバックで `est` を直す——流れは掴めたの。

**魔理沙**: 水色帯の入口だ。まとめろ。

> **第11章まとめ**
>
> - **インタラクティブ**はクエリと FEEDBACK の往復。stdout は都度 flush
> - **オンライン**では未来が見えない。推定 → 回答 → 更新のループ
> - **探索と利用**は推定の信頼と未知の辺のバランス（本格はバンディット）
> - 実装: `python/ch11/interactive_path.py` / `query_simulator.py` / `go/ch11/interactive_path.go`
> - データ: `data/ch11/sample.txt` — オフライン `queries=3 sum_pred=7.00 sum_true=19.00`
> - 次章: タブーサーチで局所探索の多様性を確保（第12章）

**霊夢**: まず `--offline` で動かしてから本番形式、ね。

**魔理沙**: その順番を守れ。インタラクティブは **1行フォーマット違いで WA** になりやすい。疑似ジャッジを自分で書けると、AHC003 系は怖くなくなるんだぜ。
