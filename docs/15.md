# 第15章 整数計画と緩和

> **到達目標（青色帯 1600〜1999）**: 割当を最適化問題として定式化し、LP 緩和と丸めでヒューリスティック解の下準備ができる。

---

## 15.1 最適化問題としての定式化

**霊夢**: 割当問題って、N 人に N 仕事を1つずつ当てるやつ？

**魔理沙**: **コスト行列 `cost[i][j]`** があり、各行・各列にちょうど1つ選ぶ。**整数計画（IP）** の典型だ。

```
  minimize  Σ_i Σ_j cost[i][j] · x[i][j]
  subject to  Σ_j x[i][j] = 1   （各行1つ）
              Σ_i x[i][j] = 1   （各列1つ）
              x[i][j] ∈ {0, 1}
```

**霊夢**: ヒューリスティック部門でも厳密に解くの？

**魔理沙**: **N が小さい部分問題** や **初期解・上界** には IP/LP が効く。本番サイズでは **緩和 + 丸め + 山登り/SA** が現実的だ。

| 段 | 本リポジトリ |
|----|-------------|
| 読み込み | `read_cost` — 先頭 `n`、続けて `n` 行 |
| データ | `data/ch15/sample.txt`（`n=4`） |

```text
4
1 4 2 8
5 3 9 1
6 7 4 2
3 8 5 6
```

---

## 15.2 線形計画法（LP）緩和の考え方

**霊夢**: 緩和って、0/1 を 0〜1 の分数にゆるめるの？

**魔理沙**: **`x[i][j] ∈ [0, 1]`** にすると **線形計画（LP）** になる。割当の LP 緩和は **「安い辺から流量を流す」** 貪欲で最適解が取れる（本リポジトリは標準ライブラリのみで実装）。

```python
# python/ch15/assignment_relax.py
def fractional_relaxation(cost):
    entries = [(cost[i][j], i, j) for i in range(n) for j in range(n)]
    entries.sort()  # コスト昇順
    row_rem = [1.0] * n
    col_rem = [1.0] * n
    for c, i, j in entries:
        val = min(row_rem[i], col_rem[j])
        x[i][j] = val
        row_rem[i] -= val
        col_rem[j] -= val
        total += c * val
```

```go
// go/ch15/assignment_relax.go — sort.Slice で同じ貪欲
func fractionalRelaxation(cost [][]int) ([][]float64, float64) {
    sort.Slice(entries, func(a, b int) bool { return entries[a].c < entries[b].c })
    ...
}
```

**霊夢**: 分数解のコストは、整数解より下回る？

**魔理沙**: **緩和は下界（最小化なら）** になる。だから **丸めた解 ≥ 緩和コスト** が目安だ（厳密な関係は問題によるが、直感として有効）。

---

## 15.3 制約付き最適化ライブラリの使い方（Python: PuLP / OR-Tools, Go: 外部 LP ソルバ連携）

**霊夢**: 本書のコードはライブラリ無し？

**魔理沙**: 第15章ハンズオンは **stdlib のみ**（依存ゼロ）。本番や研究では次を使う。

| 環境 | 選択肢 |
|------|--------|
| Python | [PuLP](https://pypi.org/project/PuLP/), [OR-Tools](https://developers.google.com/optimization) |
| Go | 外部ソルバ（GLPK, CBC）への subprocess、または Cgo |

**霊夢**: PuLP だとどう書くの？

**魔理沙**: 概念だけ示す（実行は任意インストール）。

```python
# 概念例（pip install pulp が必要）
# from pulp import LpMinimize, LpProblem, LpVariable, lpSum
# prob = LpProblem("assign", LpMinimize)
# x = {(i,j): LpVariable(f"x_{i}_{j}", cat="Binary") for i in range(n) for j in range(n)}
# prob += lpSum(cost[i][j]*x[i,j] for i in range(n) for j in range(n))
# 各行・各列の等式制約を追加 → prob.solve()
```

**Go** は **MIP を外部に投げる** 設計が多い。ヒューリスティック本体は Go、**部分問題だけ Python LP** という分担も AHC である。

---

## 15.4 緩和解からヒューリスティック解への変換

**霊夢**: 分数 `x[i][j]=0.5` みたいなのを、どう整数にするの？

**魔理沙**: 本リポジトリは **「分数が大きい (i,j) から貪欲に丸める」** 方式だ。

```python
def round_assignment(cost, frac):
    items = [(frac[i][j], cost[i][j], i, j) for i in range(n) for j in range(n)]
    items.sort(reverse=True)  # 分数の大きい順
    assign = [-1] * n
    used_col = [False] * n
    for _, c, i, j in items:
        if assign[i] != -1 or used_col[j]:
            continue
        assign[i] = j
        used_col[j] = True
        total += c
```

**霊夢**: 丸めたあと改善する余地は？

**魔理沙**: **2-opt スワップ**（行の割当を入れ替え）や **山登り** で `rounded_cost` を下げる。緩和は **初期解の種**、最終品質はヒューリスティック次第だ。

| 手法 | 期待 |
|------|------|
| 緩和のみ | 速い・下界に近い値 |
| 丸め | 整数解・速いが最適とは限らない |
| 丸め + 局所探索 | 本番向き |

---

## 15.5 ハンズオン: 割当問題を LP で近似し、整数解へ丸める

```bash
python python/ch15/assignment_relax.py < data/ch15/sample.txt
go run go/ch15/assignment_relax.go < data/ch15/sample.txt
```

検証済み出力:

```text
fractional_cost 14.00
rounded_assign [0, 3, 2, 1] cost 14
optimal_assign [2, 1, 3, 0] cost 10
n=4 gap=4 ratio=1.400
```

**霊夢**: 最適は 10 なのに、丸めは 14。gap=4 ね。

**魔理沙**: **緩和コスト 14** は分数解の値。**丸め 14** はこの貪欲丸めの結果。**全探索 `brute_optimal`** で真の最適 10 を確認している。

```python
def brute_optimal(cost):
    for perm in itertools.permutations(range(n)):
        val = sum(cost[i][perm[i]] for i in range(n))
        ...
```

**霊夢**: n=4 だから全探索できたの。本番は無理？

**魔理沙**: **部分問題だけ brute**、全体は SA やタブーで改善する。`gap` をログに残し、丸め戦略を変えた効果を測れ。

### gap の読み方

| 量 | サンプル値 | 意味 |
|----|-----------|------|
| `fractional_cost` | 14.00 | LP 緩和の最適値（下界の目安） |
| `rounded` cost | 14 | 貪欲丸めの整数解 |
| `opt_cost` | 10 | 全探索の真の最適 |
| `gap` | 4 | 丸め解 − 最適 |

**霊夢**: 緩定 14 と丸め 14 が同じなのに、最適は 10 なのは矛盾してない？

**魔理沙**: **緩和は分数の配置**、丸めは **別の整数ルール** だから一致しないことがある。`ratio=1.400` は丸めが最適の 1.4 倍コストという意味だ。

```python
# main の出力部
gap = rounded_cost - opt_cost
print(f"n={n} gap={gap} ratio={rounded_cost / max(opt_cost, 1):.3f}")
```

### ハンズオン 4 ステップ

1. 上記コマンドで 4 行の出力を確認
2. `round_assignment` のソートキーを **コスト最小優先** に変え、`gap` を比較
3. 丸め後に **隣接 swap 山登り** を 10 手だけ入れ、`cost` が 10 に届くか試す
4. `n=8` の乱数行列を生成し、緩和+丸めの時間と `gap` を記録（全探索はしない）

### 整数解への追加改善（概念コード）

```python
def local_swap_improve(cost, assign, steps=20):
    n = len(assign)
    total = sum(cost[i][assign[i]] for i in range(n))
    for _ in range(steps):
        improved = False
        for i in range(n):
            for j in range(i + 1, n):
                ni, nj = assign[i], assign[j]
                delta = (cost[i][nj] + cost[j][ni]) - (cost[i][ni] + cost[j][nj])
                if delta < 0:
                    assign[i], assign[j] = nj, ni
                    total += delta
                    improved = True
        if not improved:
            break
    return assign, total
```

**霊夢**: これを足せば gap が縮みそうね。

**魔理沙**: 演習3で試せ。青帯は **緩和 + 丸め + 局所探索** まで一続きで書く癖を付ける。

---

## 15.6 演習

1. **Hungarian 法**（O(n³)）を調べ、同一 `data/ch15/sample.txt` で最適 10 と一致するか外部実装と比較する。
2. PuLP または OR-Tools で **整数割当** を解き、`brute_optimal` と一致するか確認する。
3. 緩和のみのコスト `fractional_cost` と、丸め後 `rounded_cost`、最適 `opt_cost` を表にし、**gap の内訳** を言葉で説明する。
4. 第10章パイプラインに「緩和で初期 `assign` → 2-opt 改善」を1段挿入する設計図を描く。
5. AHC の **配置・割当系** 過去問を1つ選び、どの変数を `x[i][j]` に対応づけるか書く。

```bash
# 緩和 vs 最適の再確認
python python/ch15/assignment_relax.py < data/ch15/sample.txt | grep -E 'fractional|rounded|optimal|gap'
```

---

## 章末まとめ

**霊夢**: 緩和は下界、丸めは整数解、あとはヒューリスティックで仕上げ——青帯の入口ね。

**魔理沙**: 第5部の第一歩だ。

> **第15章まとめ**
>
> - **定式化**: 割当は 0/1 変数と行・列の等式制約
> - **LP 緩和**: `x∈[0,1]` — コスト昇順貪欲で分数最適（stdlib 実装）
> - **ライブラリ**: 本番は PuLP / OR-Tools、Go は外部ソルバ連携が一般的
> - **変換**: 分数の大きい辺から丸め → 局所探索で改善
> - 実装: `python/ch15/assignment_relax.py` / `go/ch15/assignment_relax.go`
> - 検証: `optimal cost 10`, `rounded 14`, `gap=4`, `fractional_cost 14.00`
> - 次章: 遺伝的アルゴリズムと母集団探索（第16章）

**霊夢**: gap=4 は丸めが弱かった例として覚えとくの。

**魔理沙**: **緩和だけでは提出に足りない** ことも多い。青帯では **緩和で種を作り、第8〜12章のメタヒューリスティックで仕上げる** パイプラインを組む。次は GA で **多様な母集団** から同じ種を増やすんだぜ。
