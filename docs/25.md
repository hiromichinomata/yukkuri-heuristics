# 第25章 高度なインタラクティブ・オンライン

> **到達目標（橙色帯 2400〜2799）**: **多腕バンディット**（UCB1, Thompson Sampling, ε-greedy）で辺・クエリの探索/利用を制御できる。**AHC003 系** の重み更新戦略を3種比較し、クエリ効率の考え方を説明できる。

---

## 25.1 多腕バンディット（UCB, Thompson Sampling）

**霊夢**: 第11章でもバンディットって出てきたけど、橙では何が違うの？

**魔理沙**: 第11章は **入門**。第25章は **戦略の比較実装** — 同じグラフ・同じクエリ列で UCB1 / Thompson / ε-greedy を並べる。

| 戦略 | 探索の仕方 |
|------|-----------|
| **UCB1** | 平均報酬 + 信頼区間幅（√log/t） |
| **Thompson** | ベータ分布からサンプル |
| **ε-greedy** | ε でランダム、否则 最大平均 |

```python
def exploration_bonus(self, key: Tuple[int, int]) -> float:
    st = self.stats[key]
    if st.pulls == 0:
        return 2.0
    if self.strategy == "ucb1":
        return 0.5 * math.sqrt(math.log(self.total_pulls + 1) / st.pulls)
    if self.strategy == "thompson":
        sample = self.rng.betavariate(st.alpha, st.beta)
        mean = st.reward_sum / st.pulls if st.pulls else 0.5
        return 0.8 * (sample - mean)
    if self.strategy == "epsilon_greedy":
        if self.rng.random() < self.epsilon:
            return 1.5
    return 0.0
```

**霊夢**: 「腕」は何に対応する？

**魔理沙**: AHC003 では **辺の重み推定** や **次に調べる辺** など。第25章サンプルは **推定重み付きグラフ上の最短路** + **辺ごとの pull 統計** だ。

---

## 25.2 敵対的オンライン学習の基礎

**霊夢**: 敵対的って、入力が最悪に来るってこと？

**魔理沙**: **アルゴリズムの性能を最悪ケースで評価** する枠組み。競技では「隠しテストが難しい」≒ 敵が強い、と捉える。

| 概念 | AHC での意味 |
|------|-------------|
| 後悔（regret） | 真の最短路と予測の差 |
| 探索 | 未確かめの辺を通す |
| 利用 | 今の推定で最短を出す |

```python
def update_estimates(self, path, predicted, actual):
    ratio = actual / predicted
    for i in range(len(path) - 1):
        k = edge_key(path[i], path[i + 1])
        self.est[k] = max(0.1, self.est.get(k, 1.0) * ratio)
    focus = self.pick_focus_edge(path)
    err = max(0.0, min(1.0, abs(actual - predicted) / max(actual, 1.0)))
    reward = 1.0 - err
    # stats[focus] に pulls / alpha / beta 更新
```

**霊夢**: ずっと探索ばかりだとスコア悪くない？

**魔理沙**: **バランス** が本体。UCB は理論的に後悔が √T 程度、ε-greedy は ε を減らさないと探索しすぎ、など。本番は **クエリ数 Q** に合わせて ε や UCB 定数を調整する。

---

## 25.3 クエリ効率と情報理論的な上限

**霊夢**: クエリ回数、有限だよね？

**魔理沙**: AHC003 系は **1クエリ1最短路** が基本。情報量は「返ってきた真の距離とパス」程度。

| 問い | 設計への効き |
|------|-------------|
| 1クエリで何が分かる？ | 経路上の辺の真コスト |
| 更新は線形？乗法？ | `est *= ratio`（第25章） |
| Q が小さい | 積極探索（UCB/Thompson） |
| Q が大きい | 利用寄り + キャッシュ最短路 |

```python
def run_offline(self, true_adj, queries):
    for s, t in queries:
        pred, path = self.answer(s, t)           # 推定グラフでダイクストラ
        true_len, true_path = dijkstra(true_adj, s, t)
        self.update_estimates(true_path, pred, true_len)
```

**霊夢**: 情報理論的な上限って計算するの？

**魔理沙**: 理論競技では **エントロピー** 等を使うが、橙の実務は **「Q 回で全辺を1回ずつ触れるか」** のチェックが先。触れない辺は **prior（初期推定1.0）** のまま残る。

---

## 25.4 ハンズオン: AHC003 系問題の高度な重み更新戦略

**霊夢**: フル AHC003 は重いけど、手元用は？

**魔理沙**: `data/ch25/sample.txt` — 6頂点9辺、3クエリ。`bandit_compare.py` / `bandit_compare.go` が3戦略を順に走らせる。

入力形式:

```
6 9
0 1 4.0
...
3        # クエリ数
0 5
1 4
2 5
```

### Step 1: 実行

```bash
python python/ch25/bandit_compare.py data/ch25/sample.txt
go run go/ch25/bandit_compare.go data/ch25/sample.txt
```

stderr 例:

```
graph n=6 m=9 queries=3
strategy=ucb1 sum_pred=... sum_true=19.0000 gap=...
strategy=thompson sum_pred=... sum_true=19.0000 gap=...
strategy=epsilon_greedy sum_pred=... sum_true=19.0000 gap=...
best_gap=ucb1 (smallest pred-true gap)
```

`sum_true` は **真グラフ** 上の最短路長合計。`gap = sum_pred - sum_true` が小さいほど推定が真値に近い。

### Step 2: ダイクストラ + 探索 bonus

```python
def adj(self) -> List[List[Tuple[int, float]]]:
    adj = [[] for _ in range(self.n)]
    for (u, v), w in self.est.items():
        bonus = self.exploration_bonus((u, v))
        adj[u].append((v, max(0.01, w - bonus)))
        adj[v].append((u, max(0.01, w - bonus)))
    return adj
```

探索 bonus を **重みから引く** → 未探索辺が選ばれやすい。

### Step 3: 改変

```python
# epsilon を 0.15 → 0.05 / 0.30
# UCB 係数 0.5 → 1.0
# クエリを data/ch25/sample.txt に行追加
```

Go:

```go
solver := newBanditSolver(n, topo, name, rng)
pred, true := solver.runOffline(trueAdj, queries)
```

### Step 4: コンテスト形式

1. 各クエリ前に **推定グラフ** を stderr ログ（任意）
2. **flush** してからクエリ送信（第11章）
3. 終了後に3戦略のうち **本番で使う1つ** を決め、理由を1行

---

## 25.5 演習

1. **ε = 0.05, 0.15, 0.30** で `epsilon_greedy` の gap を比較する。
2. クエリを **5本** に増やした `sample.txt` を自作し、UCB と Thompson の gap 差を観察する。
3. 第11章 AHC003 解法と第25章の **更新式（乗法 ratio）** の違いを3点書く。
4. 第26章予習: **ビジュアライザ** で「どの辺が pull されたか」を色付けする案をスケッチする。

```bash
python python/ch25/bandit_compare.py data/ch25/sample.txt 2>&1 | tee bandit.log
grep strategy bandit.log
go run go/ch25/bandit_compare.go data/ch25/sample.txt 2>&1 | grep best_gap
```

---

## 章末まとめ

**霊夢**: インタラクティブは、探索と利用のバランス取りが全部なんだね。

**魔理沙**: まとめだ。

> **第25章まとめ**
>
> - **UCB1 / Thompson / ε-greedy** を同条件で比較（`bandit_compare`）
> - 敵対的枠組み → **後悔 = 予測と真値の差**
> - **クエリ効率**: Q に対して触れる辺数・更新式（乗法 ratio）
> - ハンズオン: `data/ch25/sample.txt`（6頂点・3クエリ）
> - 次章: **ビジュアライザ駆動開発**（第26章）

**霊夢**: 辺の pull 状況、見える化したらデバッグ楽そう。

**魔理沙**: 第26章で **自作ビジュアライザ** の設計に入る。LNS の destroy 区間を描くのも相性がいいぜ。
