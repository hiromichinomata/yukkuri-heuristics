# 第19章 モンテカルロ木探索（MCTS）

> **到達目標（黄色帯 2000〜2399）**: UCB1 で **探索と利用** を両立し、ターン制問題に MCTS を実装できる。時間内の rollout 回数とポリシー選択のトレードオフを説明できる。

---

## 19.1 UCB1 と探索 / 利用

**霊夢**: 貪欲はいつも一番良さそうな手を取るけど、MCTS は違うの？

**魔理沙**: **Monte Carlo Tree Search** は、試した回数の少ない手も試す。**UCB1** で「期待利得 + 探索ボーナス」を最大化する子を選ぶ。

\[
\text{UCB1}(i) = \frac{w_i}{n_i} + c \sqrt{\frac{\ln N}{n_i}}
\]

| 記号 | 意味（第19章） |
|------|----------------|
| \(w_i\) | 子ノードの累積勝利（`wins`） |
| \(n_i\) | 子の訪問数（`visits`） |
| \(N\) | 親の訪問数 |
| \(c\) | 探索定数（`EXPLORATION = 1.41`） |

```python
# python/ch19/mcts_toy.py
def ucb1(parent_visits: int, child: MCTSNode) -> float:
    if child.visits == 0:
        return float("inf")  # 未訪問を最優先
    return child.wins / child.visits + EXPLORATION * math.sqrt(
        math.log(parent_visits) / child.visits
    )
```

```go
// go/ch19/mcts_toy.go — 同じ式で best child を選択
```

**霊夢**: `visits == 0` が inf なのは？

**魔理沙**: **各手を最低1回は試す** ため。探索不足で強い手だけに偏るのを防ぐ。

---

## 19.2 ゲーム木とシミュレーション

**霊夢**: 木って全部先読みするの？

**魔理沙**: **全部は無理**。4 フェーズで **必要な枝だけ** 伸ばす。

```
1. Selection   … ルートから UCB1 で葉へ
2. Expansion   … 未試行の手で子ノードを1つ追加
3. Simulation  … ランダム（または貪欲）で終局まで rollout
4. Backprop    … 経路上の visits / wins を更新
```

```python
def mcts(root, adj, game, rng):
    for _ in range(ITERATIONS):
        node = root
        path = [node]
        # Selection: 子が全部展開済みなら UCB1 で降りる
        while not node.untried and node.children:
            node = best_child(node)
            path.append(node)
        # Expansion
        if node.untried:
            node = expand(node, adj, rng)
            path.append(node)
        # Simulation (rollout)
        winner = rollout(node.state, adj, game, rng)
        # Backpropagation
        for n in path:
            n.visits += 1
            if winner == n.state.player:
                n.wins += 1.0
```

**霊夢**: 第19章の「ゲーム」は？

**魔理沙**: **4 ノードの無向グラフ**（`data/ch19/sample.txt`）。`start=0`、ノード `3` に着いたプレイヤーが勝ち。ターンごとに MCTS で1手選び、最大 `MAX_TURNS` 手。

---

## 19.3 ヒューリスティック問題への MCTS 適用

**霊夢**: AHC はスコア最大化なのに、勝敗？

**魔理沙**: ヒューリスティックでも **ターン制・対戦型** なら MCTS が使える。単独最適化なら **rollout の終端スコア** を `wins` に足す形に変える。

| 問題型 | MCTS の状態 | rollout 終端 |
|--------|-------------|--------------|
| 対戦（Territory 等） | 盤面 + 手番 | 勝敗 / スコア差 |
| ターン制単独 | 操作列 + 残ターン | `score_schedule` 等 |
| 連続操作 | 部分解 | 貪欲で埋めて評価 |

第17章 Territory では「自分の手を選ぶ → 相手も動く」を木に載せられる。計算量が重いので **浅い深さ + 強い rollout ポリシー** が現実的だ。

**霊夢**: 第19章コードは対戦だけ？

**魔理沙**: 教学用に **勝敗がはっきりしたグラフ** にしている。応用時は `terminal_winner` を **スコア閾値** に差し替えればよい。

```python
def terminal_winner(state, game):
    if state.node == game.terminal:
        return state.player
    return None
```

---

## 19.4 時間制限内での rollout 回数

**霊夢**: `ITERATIONS = 300` 固定？

**魔理沙**: ハンズオン用の固定値だ。本番は **残り時間まで** `while timer:` で MCTS を回す。1 イテレーションのコストは **展開 + rollout 長** で決まる。

| 調整ノブ | 効果 |
|---------|------|
| イテレーション数 ↑ | 手の評価が安定、時間↑ |
| rollout 深さ ↓ | 速いが評価がノイジー |
| `EXPLORATION` ↑ | 探索寄り（未試行の手を多く見る） |
| `EXPLORATION` ↓ | 利用寄り（高 win 率の手に集中） |

```python
EXPLORATION = 1.41  # √2 がよく使われる
ITERATIONS = 300      # 本番では while time_ok: の回数に置換
```

**霊夢**: 1 手だけ MCTS して選ぶ？

**魔理沙**: `play_game` は **毎ターン root を新規構築** し、`select_move` で訪問数最大の子を選ぶ（robust child）。

```python
mv = max(root.children, key=lambda m: root.children[m].visits)
```

---

## 19.5 ハンズオン: ターン制問題に MCTS を実装する

**霊夢**: 入力の読み方は？

**魔理沙**: `data/ch19/sample.txt`:

```
4 4        # nodes, edges
0 1
1 2
...
0          # start node
```

### Step 1: 実行

```bash
python python/ch19/mcts_toy.py < data/ch19/sample.txt
go run go/ch19/mcts_toy.go < data/ch19/sample.txt
```

stderr: `mcts moves=16 path=1 0 1 0 ...`。stdout は各ターンの次ノード ID。

### Step 2: イテレーション数

```python
ITERATIONS = 100
ITERATIONS = 1000
```

同じ seed (`42`) で **手列がどう変わるか** を見る。

### Step 3: rollout ポリシー（19.6 と共通）

```python
def rollout(state, adj, game, rng):
    # 現状: ランダム合法手
    cur = apply_move(cur, rng.choice(moves))

# 改変案: grid 値が高い隣接へ貪欲（第17章の best_move 相当）
```

### Step 4: 第17章・第20章との接続

| 章 | 接続 |
|----|------|
| 第17章 | マルチエージェントの手番 → MCTS の層 |
| 第20章 | 時間を複数 SA に分割するのと同様、**時間を MCTS イテレーションに分割** |
| 第8章 SA | 操作列全体を焼く別ルート |

---

## 19.6 演習

1. `EXPLORATION` を `0.5 / 1.41 / 2.5` に振り、**同じ入力での手列** を比較する。
2. rollout を **ランダム vs 貪欲（次数最小の手など）** に変え、必要イテレーション数の違いをメモする。
3. [AHC008](https://atcoder.jp/contests/ahc008) を思い出し、**3 手読みの貪欲** と **1 手 MCTS** の計算量を見積もる（実装不要）。

```bash
python python/ch19/mcts_toy.py < data/ch19/sample.txt 2> mcts.log
cat mcts.log
```

---

## 章末まとめ

**霊夢**: 試行回数で手の強さを測る——貪欲より信頼できそう。

**魔理沙**: まとめだ。

> **第19章まとめ**
>
> - MCTS は **Selection → Expansion → Simulation → Backprop** の繰り返し
> - **UCB1** が探索と利用のバランスを取る（未訪問子は優先）
> - ヒューリスティックでは rollout 終端を **スコア** に置き換えられる
> - 本番は **残り時間でイテレーション数** を決める
> - ハンズオン: `mcts_toy`（4 ノードグラフ・対戦）
> - 次章: **並列 multi-start SA**（第20章）

**霊夢**: 次は並列で焼きなまし？

**魔理沙**: 1 本の SA より **シード違い4本** の方が伸びることが多い。黄色帯の定番テクだぜ。
