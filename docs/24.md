# 第24章 大近傍探索（Large Neighborhood Search）

> **到達目標（橙色帯 2400〜2799）**: **広い近傍** を **破壊（destroy）と再構築（repair）** で設計できる。TSP で LNS を実装し、**破壊率とスコア・時間のトレードオフ** を測れる。

---

## 24.1 広い近傍の設計

**霊夢**: 2-opt や1日 swap じゃ足りないときは？

**魔理沙**: **一度にたくさんの要素を入れ替える** — それが **Large Neighborhood Search（LNS）** の「大」だ。第18章 SA の近傍ポートフォリオの延長でもある。

| 近傍 | 変更規模 | 例 |
|------|---------|-----|
| 小 | 1〜2要素 | 2-opt, 1日 swap |
| 大 | 区間・集合 | 経路の連続 k 頂点削除して再挿入 |

**霊夢**: 大きく壊すと合法解が壊れない？

**魔理沙**: **repair** で必ず直す。第24章 TSP では「巡回順序」だけなので、削除した頂点を **最近傍貪欲** で戻せば合法巡回になる。

```python
def destroy_segment(tour: List[int], rng: random.Random) -> Tuple[List[int], List[int]]:
    n = len(tour)
    seg_len = rng.randint(1, max(1, n // 3))  # 破壊率 ≒ 1/3 まで
    start = rng.randint(0, n - 1)
    removed = []
    kept = list(tour)
    for _ in range(seg_len):
        idx = start % len(kept)
        removed.append(kept.pop(idx))
        start += 1
    return kept, removed
```

---

## 24.2 部分問題を最適化 / ヒューリスティックに解く

**霊夢**: 壊したあと、全部最適化するの？

**魔理沙**: **部分問題のサイズ** に応じて選ぶ。

| 部分問題サイズ | repair 手法 |
|---------------|------------|
| 小（≤10） | 全列挙・DP |
| 中 | 貪欲・2-opt |
| 大 | ヒューリスティック + 局所改善 |

第24章は **最近傍法（nearest neighbor）** で repair:

```python
def nearest_neighbor_tour(pts, start: int, nodes: List[int]) -> List[int]:
    remaining = set(nodes)
    remaining.discard(start)
    tour = [start]
    cur = start
    while remaining:
        nxt = min(remaining, key=lambda j: dist(pts, cur, j))
        remaining.remove(nxt)
        tour.append(nxt)
        cur = nxt
    return tour
```

**霊夢**: 配置問題だと？

**魔理沙**: 矩形 k 個を外して **空きマス貪欲** で戻す、など同型。第23章で見抜いた **本質構造** が repair の設計図になる。

---

## 24.3 破壊と再構築（Destroy & Repair）

**霊夢**: LNS の1イテレーションの流れは？

**魔理沙**: 定番ループだ。

```
current tour
  → destroy（区間削除）
  → repair（部分 TSP）
  → 良ければ current を更新
  → best-so-far も更新
```

```python
def lns(pts, rng, iterations=200):
    tour = list(range(n))
    rng.shuffle(tour)
    initial = tour_length(pts, tour)
    best, best_len = list(tour), initial

    for it in range(iterations):
        partial, removed = destroy_segment(tour, rng)
        candidate = repair_tour(pts, partial, removed)
        cand_len = tour_length(pts, candidate)
        if cand_len + 1e-9 < tour_length(pts, tour):
            tour = candidate
        if cand_len + 1e-9 < best_len:
            best, best_len = candidate, cand_len
    return best, initial, best_len
```

Go も同型:

```go
func lns(pts []Point, rng *rand.Rand, iterations int) ([]int, float64, float64) {
    partial, removed := destroySegment(tour, rng)
    candidate := repairTour(pts, partial, removed)
    candLen := tourLength(pts, candidate)
    // ...
}
```

| 受理条件 | 効果 |
|---------|------|
| 改善のみ（第24章） | 安定、初期解依存 |
| SA 受理 | 多様性（第8章と合成） |
| 常に受理 + 多スタート | 探索幅↑ |

---

## 24.4 ハンズオン: 経路問題の LNS 実装

**霊夢**: 入力形式は？

**魔理沙**: `data/ch24/sample.txt` — 1行目 n、続く n 行が (x, y)。8点のユークリッド TSP。

```
8
0 0
4 1
...
```

### Step 1: 実行

```bash
python python/ch24/lns_tsp.py < data/ch24/sample.txt
go run go/ch24/lns_tsp.go < data/ch24/sample.txt
```

出力例:

```
lns it=50 current=28.98 best=28.98
tour: 3 4 5 6 7 0 1 2
n=8 before=48.49 after=28.98 improved=19.50
```

ランダム初期巡回 **48.49** → LNS 後 **28.98**（約40%改善）。

### Step 2: ログを読む

50イテレーションごとに stderr:

```python
if (it + 1) % 50 == 0:
    print(f"lns it={it+1} current={...} best={best_len:.2f}", file=sys.stderr)
```

### Step 3: 改変 — 破壊率

```python
seg_len = rng.randint(1, max(1, n // 3))   # デフォルト
# seg_len = rng.randint(1, max(1, n // 2))  # より大胆
# seg_len = 1                               # ほぼ 2-opt 相当
```

**24.5 演習** でスコアと時間を表にする。

### Step 4: コンテスト形式

1. 初期解: ランダム shuffle（`seed=42` 固定で再現）
2. 残り時間の **80%** を LNS、**20%** を 2-opt（第6章）
3. stdout に `tour:` 行 — 本番提出形式に合わせて整形

---

## 24.5 演習

1. `iterations` を `50 / 200 / 500` に振り、**after** と実行時間を記録する。
2. 破壊長 `n//3` vs `n//2` vs `1` を比較し、**improved** 列を表にする。
3. repair を **2-opt だけ** に差し替えた版を設計し、LNS との差を述べる（実装任意）。
4. 第23章 `pipeline_lite` 勝者スケジュールに **「3日連続区間を destroy して貪欲 repair」** LNS を想像で1段落書く。

```bash
# 再現実行
python python/ch24/lns_tsp.py < data/ch24/sample.txt 2> lns.log
grep best lns.log
go run go/ch24/lns_tsp.go < data/ch24/sample.txt 2>&1 | grep "after="
```

---

## 章末まとめ

**霊夢**: 大きく壊して賢く直す——LNS は SA の近傍を広げる技術ね。

**魔理沙**: まとめだ。

> **第24章まとめ**
>
> - **LNS** = destroy + repair の反復
> - 破壊: 区間削除（長さ ~ n/3）、再構築: 最近傍貪欲
> - `lns_tsp.py` / `lns_tsp.go` — `data/ch24/sample.txt`（8点 TSP）
> - **破壊率・反復数** でスコアと時間のトレードオフ
> - 次章: **高度なインタラクティブ・バンディット**（第25章）

**霊夢**: AHC003 系、バンディットで攻める章か。

**魔理沙**: クエリ効率と **UCB / Thompson** の比較が第25章だぜ。
